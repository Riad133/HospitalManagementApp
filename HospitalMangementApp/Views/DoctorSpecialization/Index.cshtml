@model IEnumerable<HospitalMangementApp.Models.DoctorSpecialization>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Specialization)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Specialization)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.DoctorSpecializationId }) |
            @Html.ActionLink("Details", "Details", new { id=item.DoctorSpecializationId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.DoctorSpecializationId })
        </td>
    </tr>
}

</table>
